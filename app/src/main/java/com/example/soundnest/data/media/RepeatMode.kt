package com.example.soundnest.data.media

enum class RepeatMode {
    OFF, // play playlist one time (Default).
    ALL, // repeat playlist's all songs after playlist complete.
    ONE, // repeat current song.
}